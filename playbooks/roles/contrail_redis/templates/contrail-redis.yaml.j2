version: '2'
services:
  redis:
    image: "{{ container_registry }}/{{ contrail_container_prefix }}-external-redis:{{ contrail_version_tag }}"
    env_file: /etc/contrail/common.env
    network_mode: "host"
    restart: always
